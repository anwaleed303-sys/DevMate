import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "DevMate - AI Coding Assistant",
  description:
    "Intelligent code analysis, refactoring, and suggestions powered by AI",
  keywords: "AI, coding assistant, code analysis, refactoring, developer tools",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
